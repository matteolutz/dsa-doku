server {
  listen 80;

  server_name dsa-doku-frontend;

  root /usr/share/nginx/html;
  index index.html

  location / {
    try_files $uri /index.html;
  }

  location ~* \.mjs$ {
    types {
      text/javascript mjs;
    }
  }
}
