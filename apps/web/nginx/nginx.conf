server {
 listen 80;

 server_name dsa-doku-frontend;

 location / {
   root /usr/share/nginx/html;
   try_files $uri /index.html;
 }

 location ~* \.mjs$ {
    types {
      text/javascript mjs;
    }
  }
}
